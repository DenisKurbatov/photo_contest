<li>
<div class = "comment">
  <div class="row title">
    <div class="col-md-6">
      <% author = User.find(comment.user_id)%>
      <%= author.name %>:
    </div>
    <div class="col-md-6 text-right">
      <%= comment.created_at%> 
    </div>
  </div>
  <div class="row">
    <p class = "content col-md-12 center-block"> <%= comment.body %> </p>
  </div>
  <div class="row">
    <div class="col-md-2 pull-right">
      <%=   link_to(raw("<button class='btn btn-primary pull-right'>Комментировать</button>"), 
          new_user_photo_comment_comments_path(@photo, comment_parent_type: :comment, comment_parent_id: comment.id) )%>
   
    </div>
  </div>
<div>
<% @comments = Comment.get_comments("Comment", comment.id)%>
<div class="row">
<ul class="col-md-offset-1 col-md-11 chl-com pull-right"> <%= render @comments %> </ul>
</div>
</li>