<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <%= link_to("Фотоконкурс", photos_path, method: "get", class: "navbar-brand") %>
    </div>

    <div class="navbar-collapse collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <li> <%= link_to(image_tag(current_user.image, size: "50x50", class: "img-circle"), 
                                                                    "#", class: "avatar")%></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= current_user.name%><span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Профиль", users_path(current_user.id), method: "get" %></li>
              <li><%= link_to "Мои фото", user_photos_path(current_user), method: "get" %></li>
              <li><%= link_to "Загрузить фото", new_photo_path, method: "get" %></li>
              <li class="divider"></li>
              <li><%= link_to "Выйти", logout_path, method: "delete" %></li>
            </ul>
          </li>
        <% else %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Войти <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Вконтакте", '/auth/vkontakte' %></li>
              <li><%= link_to " GitHub", '/auth/github' %></li>
            </ul>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>